# v1.5.1

## Release Date

April 13, 2026

---

## Admin

### Bug Fixes

#### Missing Redis Provider in K8s Operator

*   Fix `K8sOperator` not receiving `redis_provider` during initialization in `OperatorFactory`, which caused Redis-dependent features to be unavailable when using the Kubernetes backend ([#765](https://github.com/alibaba/ROCK/pull/765))
