# v1.5.1

## 发布日期

2026 年 4 月 13 日

---

## Admin

### Bug 修复

#### K8s Operator 缺失 Redis Provider

*   修复 `OperatorFactory` 初始化 `K8sOperator` 时未传入 `redis_provider` 的问题，导致使用 Kubernetes 后端时 Redis 相关功能不可用 ([#765](https://github.com/alibaba/ROCK/pull/765))
